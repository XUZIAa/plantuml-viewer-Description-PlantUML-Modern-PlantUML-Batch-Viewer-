@startuml
title 用户登录流程图

start

:用户访问登录页面;

:输入用户名和密码;

if (用户名和密码是否为空?) then (是)
  :显示错误提示;
  stop
else (否)
  :提交登录请求;
endif

:后端验证用户信息;

if (用户信息是否正确?) then (是)
  :生成 Token;
  :返回登录成功;
  :跳转到首页;
  stop
else (否)
  if (错误次数 > 3?) then (是)
    :锁定账户 30 分钟;
    :发送安全提醒邮件;
    stop
  else (否)
    :显示错误提示;
    :错误次数 +1;
    stop
  endif
endif

@enduml
